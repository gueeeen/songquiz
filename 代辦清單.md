# SongQuiz 代辦清單

> 這是這個專案的控制文件。做到哪、下一步做什麼，都看這裡。
> 為什麼這樣切見 `架構.md`。

---

## 〇、接續點（2026-09-15）

**已經公開發佈，手機直接開就能玩**：<https://gueeeen.github.io/songquiz/>
（repo：<https://github.com/gueeeen/songquiz>，只有 SongQuiz，手帳沒有上去）

單人與多人在**同一頁**，首頁最上面切換。單人有排行榜（這台裝置 ＋ 線上），
多人是當場玩、當場看結果，刻意不上榜。

- 開場能挑語種（多選）、題數（5／10／15／20）、模式（闖關／競速／積分）
- 三條連線路徑都驗過：同機多分頁、區域網路（`區域網路開房.cmd`）、
  跨網路（Supabase，金鑰已填在 `web\realtime-config.js`）
- 規則測試 61 項全過（`web\tests.html`）、題庫產生器 19 項全過（`dotnet test`）
- 題庫 512 首可出題、765 個誘餌，已進版控
- 端到端實跑過：合併後的單人＋多人 17／17、排行榜 10／10、自動換歌與洗盤 5／5

**下次開工先做這件事**：真的用手機玩一場。目前全部是桌機無頭瀏覽器驗的，
手機上有三件事沒人試過——iOS Safari 的自動播放限制、九個選項在小螢幕上的可讀性、
還有現場 Wi-Fi 下 Apple 試聽的載入速度。這三件只能實機驗。

**發佈流程**（本機是 monorepo，公開的是只含 SongQuiz 的分支）：

**每次發佈前先把 webindex.html 裡的 `?v=N` 加一**（全部 12 處）。
GitHub Pages 的 Cache-Control 是 max-age=600，不換版本號的話，
改完最多要等十分鐘才看得到，而且會出現「新 HTML 配舊 CSS」的混搭。

```
git subtree split --prefix=SongQuiz -b songquiz-only
git branch songquiz-publish songquiz-only
git filter-branch -f --env-filter '…改成 noreply 信箱…' songquiz-publish
git subtree split --prefix=SongQuiz/web -b songquiz-web
git push -f <repo> songquiz-publish:main
git push -f <repo> songquiz-web:gh-pages      # Pages 吃這個分支的根目錄
```

改寫作者信箱是刻意的：公開的提交裡不要出現 gmail。

---

## 一、已經做完的

| 項目 | 狀態 | 備註 |
|---|---|---|
| 開場設定：語種多選、題數、模式 | ✅ | 設定記在 localStorage |
| 闖關關卡依語種動態生成 | ✅ | k 個語種 → k+1 關，門檻按比例算 |
| 積分模式（連對加成） | ✅ | ×1→×5 封頂、答錯歸零、不看速度 |
| 開打前檢查題庫夠不夠 | ✅ | `Rules.capacityFor()`，不夠不讓開始 |
| 九選一、同語種誘餌 | ✅ | 題庫只剩三首也湊得滿九個 |
| 計分：速度加權與連對加成 | ✅ | 12.0 秒是硬邊界；積分模式刻意不看速度 |
| 新解鎖的語種保證至少出一題 | ✅ | 用偏斜題庫測過（華語 400 首、台語 1 首） |
| 個人最佳成績（localStorage） | ✅ | 按模式＋題數＋語種各記一筆 |
| 排行榜：這台裝置 ＋ 線上 | ✅ | 只有單人；多人刻意不上榜（見第二節） |
| 單人與多人同一頁 | ✅ | 首頁切換，不跳頁 |
| 多人房間（先答對得分，上限 6 人） | ✅ | 三條連線路徑：同機／區域網路／Supabase |
| 公開發佈到 GitHub Pages | ✅ | 只推 SongQuiz，提交者改成 noreply |
| 結算頁逐題回顧＋重聽該首試聽 | ✅ | |
| 歌名清洗 | ✅ | 巢狀括號清到底，1277 個歌名零殘渣 |
| 題庫產生器（iTunes Search API） | ✅ | 循序、間隔 400 毫秒 |
| 規則測試在瀏覽器裡跑 | ✅ | 沒有 Node，所以自己寫跑法 |

**架構換過一次**：第一版是「ASP.NET Core 伺服器判分 + 前端顯示」，
後來改成純靜態站。想找伺服器判分的實作，翻到提交「骨幹：兩種模式的猜歌遊戲，答案留在伺服器」。

---

## 二、下一步（依我建議的順序）

| 順位 | 項目 | 為什麼是這個 |
|---|---|---|
| 1 | **實機驗證** | 見上面接續點。沒驗過等於不知道能不能給人玩 |
| 2 | **題庫體檢工具** | 掃過每個 previewUrl 確認還活著（Apple 會換網址）。壞掉的歌會變成「播不出聲音」的題目 |
| 3 | **難度旋鈕** | 把 12 秒、9 選項、門檻比例做成設定，不用改程式 |
| 4 | **本機音檔覆蓋** | `web/audio/<id>.m4a` 存在就優先用它，現場網路爛掉也能玩 |
| 5 | **房主離開就散團** | 沒有重選房主、沒有斷線重連。房主重新載入就是一間新房 |

**排行榜只有單人那一邊有，多人刻意不上榜**（2026-09-15 決定）。

房間的分數是「搶到幾題」，和單人的分數不是同一個單位——混在一起排名沒有意義，
而一對一贏和六人場贏也不是同一件事。房間就是當場玩、當場看結果，
名次隨房間關掉一起消失。要改變這件事的話，該做的是**另外一份**「房間榜」
（記搶題率與同場人數），不是把兩種分數塞進同一張表。

**分數可信度的老實話**：線上榜的分數是玩家的瀏覽器算完送上來的，
改 JavaScript 就能送假的。要真的防作弊，判分必須搬回伺服器
（見 `架構.md` 第三節）。這個榜是給朋友之間玩的，不是競賽計分系統，
所以 RLS 只開 select 與 insert——至少改不動也刪不掉別人的紀錄。

---

## 三、多人房間與服務成本（2026-09-14 查的）

玩法：房主建房拿短房號，朋友輸入房號加入，**先答對的人得分**，用題數結算。
題目一致的做法是「房主決定一顆亂數種子」，所有人用同一顆種子跑 `Game`。

即時層做成**可替換的 adapter**，所以日後換服務或改自架，動的是一個檔案：

- `BroadcastChannelAdapter`：同一台電腦多開分頁就能玩，**不需要任何帳號**，
  是開發與測試的固定工具
- 真實服務的 adapter：金鑰填在 `web\realtime-config.js`（只放設計上可公開的 anon key）

### 流量估算與免費額度

一場 5 人 10 題約 **150 則訊息、30 KB**（廣播題目、回傳答案、結算）。

| 服務 | 免費額度 | 換算 | 真正的天花板 |
|---|---|---|---|
| Supabase Realtime | 每月 200 萬則、200 個同時連線 | 約 13,000 場/月 | **閒置 7 天專案自動暫停** |
| Firebase Realtime DB | 每月 10 GB 下載 | 約 34 萬場/月 | 同時只能 100 個連線 |
| Cloudflare Durable Objects | 每天 10 萬次請求 | 每天數百場 | 不會因閒置暫停；部署要 Node（這台沒有） |

**結論：流量永遠不會是問題，差三四個數量級。** 會逼人搬家的是政策
（Supabase 的閒置暫停），不是用量。

### 什麼時候該自架

| 方案 | 價格 | 什麼時候 |
|---|---|---|
| Hetzner CX22 | 約 €3.79/月 | 變成常態活動、不想再看別人的政策臉色 |
| Oracle 永久免費 ARM | $0 | 願意折騰；注意 2026-06-15 免費額度砍半成 2 OCPU/12GB，閒置實例會被回收 |
| Cloudflare Durable Objects | 免費額度內 $0 | 長期最省事：房間閒置會休眠不計費、不暫停 |

自架的話，第一版的 ASP.NET Core 伺服器判分實作在提交「骨幹：兩種模式的猜歌遊戲，答案留在伺服器」，可以翻回去接。

---

## 四、已知的坑

- **答案在瀏覽器裡**：純靜態的代價，和參考站一樣。見 `架構.md` 第三節。
- **iOS 自動播放**：瀏覽器要求使用者手勢才能播音訊。現在靠「選模式／下一題」
  那一下當手勢，理論上成立，但沒在 iPhone 上驗過。
- **Apple 試聽網址會過期**：`previewUrl` 不保證永久有效。題庫放久了要重建。
- **題庫太瘦時兩個小問題**（正常題庫不會遇到，都不值得現在加程式）：
  1. 誘餌不足時選項會少於九個（誘餌是湊滿九個的靠山）。要變成硬保證得由
     產生端保證誘餌數量下限。
  2. 題庫湊不出題而遊戲提早結束時，闖關模式的結算標題會誤寫成「闖關失敗」。
- **Smart App Control 會擋剛寫入的檔案**：這台機器的 SAC 開在強制執行模式，
  封鎖「剛產生、還沒有信譽」的執行檔與 DLL，過幾分鐘會自己放行
  （機制與實測見 `..\MofuCal\代辦清單.md` 的「環境備忘」）。所以不要用
  `dotnet run`，一律走 `重建題庫.cmd`。遇到就等一下再跑，不要去改程式。
  好消息是玩遊戲完全不受影響——它只是網頁。

---

## 五、不做的事

- **不自己存音檔。** 播放一律用 Apple 官方試聽網址。這不是技術取捨，是授權取捨。
- **不做帳號系統。**
- **不引入打包工具或前端框架。** 這台機器沒有 Node，而這個站也不需要——
  傳統 script 才能讓 `file://` 雙擊就玩得起來。
