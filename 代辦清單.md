# SongQuiz 代辦清單

> 這是這個專案的控制文件。做到哪、下一步做什麼，都看這裡。
> 為什麼這樣切見 `架構.md`。

---

## 〇、接續點（2026-09-13 開工第一天）

**骨幹跑得起來，53 個測試全過。** 已經可以玩：兩種模式、九選一、
伺服器判分、逾時零分、闖關過關與失敗、結算逐題回顧。

題庫已建：**512 首可出題、765 個誘餌**，橫跨華語 160／西洋 112／台語 80／
韓語 80／日語 80。

**下次開工先做這件事**：真的用手機連上去玩一場。目前只在桌機瀏覽器與 curl 驗過，
手機上有三件事沒人試過——iOS Safari 的自動播放限制、九個選項在小螢幕上的可讀性、
還有現場 Wi-Fi 下 Apple 試聽的載入速度。這三件都只能實機驗。

---

## 一、已經做完的

| 項目 | 狀態 | 備註 |
|---|---|---|
| 四層專案骨架與依賴方向 | ✅ | 編譯器強制，Quiz 不知道有遊戲 |
| 題庫模型與讀寫（`bank.json`） | ✅ | 沒有試聽網址的歌讀進來就丟掉 |
| 歌名清洗 | ✅ | 巢狀括號清到底，1277 個歌名零殘渣 |
| 出題：九選一、同語種誘餌 | ✅ | 題庫只剩三首也湊得滿九個 |
| 計分：秒答 1000、滿分 10000 | ✅ | 12.0 秒是硬邊界 |
| 闖關：六關、門檻、語種解鎖 | ✅ | 新解鎖語種保證至少一題 |
| 競速：十題 | ✅ | |
| 伺服器 API 與對局管理 | ✅ | 記憶體，兩小時清 |
| 前端：開場／遊戲／結算三畫面 | ✅ | 手機兩欄、桌機三欄 |
| 題庫產生器（iTunes Search API） | ✅ | 循序、間隔 400 毫秒 |

---

## 二、下一步（依我建議的順序）

| 順位 | 項目 | 為什麼是這個 |
|---|---|---|
| 1 | **實機驗證** | 見上面接續點。沒驗過等於不知道能不能擺攤 |
| 2 | **排行榜** | 現在分數算完就沒了。擺攤要的是「今天誰最強」，這是最有感的缺口 |
| 3 | **本機音檔覆蓋** | `audio/<trackId>.m4a` 存在就優先用它，現場網路爛掉也能玩 |
| 4 | **答錯的歌可以再聽一次** | 結算頁每首歌加一個播放鈕，玩完想知道「剛剛那首到底是什麼」 |
| 5 | **題庫體檢工具** | 掃過每個 previewUrl 確認還活著（Apple 會換網址） |
| 6 | **難度旋鈕** | 把 12 秒、9 選項、六關門檻做成設定，不用改程式 |

**排行榜的取捨要先想清楚**：一旦要跨場次留分數，就需要存檔（SQLite 或一個 JSON），
而現在整個伺服器是無狀態的。建議只存「暱稱＋分數＋模式＋時間」四個欄位，
不要把對局本身寫進去——對局是暫時的，分數才是要留的。

---

## 三、已知的坑

- **iOS 自動播放**：瀏覽器要求使用者手勢才能播音訊。現在靠「選模式／下一題」
  那一下當手勢，理論上成立，但沒在 iPhone 上驗過。
- **Apple 試聽網址會過期**：`previewUrl` 不保證永久有效。題庫放久了要重建。
- **Smart App Control 會擋剛寫入的檔案**：這台機器的 SAC 開在強制執行模式，
  封鎖的是「剛產生、還沒有信譽」的執行檔與 DLL，過幾分鐘到幾十分鐘會自己放行
  （機制與實測記錄見 `..\代辦清單.md` 的「環境備忘」，MofuCal 已經查清楚）。
  實際踩到的兩次：`dotnet run` 要啟動新編的 .exe（存取被拒）、
  以及搬家後重編的 `SongQuiz.Game.Tests.dll` 被擋（0x800711C7）。
  所以腳本一律走「dotnet 主機載入 DLL」，機率較低但不是免疫——
  遇到就等一下再跑，不要去改程式。
- **啟動時的工作目錄會影響 ContentRoot**：題庫路徑靠 `BankPath.Resolve`
  在 ContentRoot 與執行檔目錄兩處都找，但 `wwwroot` 沒有這層保護——
  所以腳本都會先 `cd` 進 `src\SongQuiz.Server`。

---

## 四、不做的事

- **不自己存音檔。** 播放一律用 Apple 官方試聽網址。這不是技術取捨，是授權取捨。
- **不做帳號系統。** 擺攤的遊戲，輸個暱稱就夠了。
- **不把對局寫進資料庫。** 見 `架構.md` 第五節。
