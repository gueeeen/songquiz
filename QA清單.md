# QA 清單

三套自動測試（`web/tests.html` 81 項、`web/layout-tests.html` 55 項、
`web/playthrough.html` 14 項）全部是**無頭瀏覽器在這台 Windows 上**跑的。
它們驗得到規則、版面、和「一整場走不走得完」，但有一整類東西驗不到：

- **真的發得出聲音嗎。** 無頭環境沒有音效裝置，測試裡的音訊一律走「放不出來」那條路。
- **iOS Safari。** 它的自動播放限制和桌機 Chromium 不是同一套規則，而這個遊戲整個
  建立在「按下開始的那一下手勢，可以帶著後面九題的播放」這個假設上。
- **真的兩台裝置連得上嗎。** 多人房的同機測試用的是 BroadcastChannel，
  跨裝置走的是 Supabase Realtime，那是完全不同的一條路。
- **現場的網路。** Apple 的試聽是從 Apple 的伺服器直接串的，攤位的 Wi-Fi 下要多久才出聲。

底下每一項都要**真的用手拿著手機做一次**。做完在前面打勾，並把發現寫在最後。

---

## 一、單人（iPhone + Safari）

- [ ] 用 Safari 開 <https://gueeeen.github.io/songquiz/>，等題庫載完（下方那行不再是「載入中」）
- [ ] 右上角齒輪 → 音量拉到一半 → 按「試聽」，**有沒有聲音**
- [ ] 關掉設定 → 按「開始」→ 第一題**幾秒內出聲**（這是最可能壞掉的一項）
- [ ] 倒數是在**出聲之後**才開始跑，不是在按下開始的當下
- [ ] 九個選項在直立握持時，**不用捲動**就全部看得到
- [ ] 歌名比歌手明顯大、而且各佔一行
- [ ] 隨便點一個 → 答對／答錯的顏色一眼分得出來
- [ ] 一路打完十題 → 走到結算頁
- [ ] 結算頁每一列的「重聽」按下去有聲音
- [ ] 按「再來一場」→ 直接開下一場，上一場的回顧已經清掉

## 二、鎖定螢幕與切換 App（iOS 最容易出事的地方）

- [ ] 遊戲進行中按下電源鍵鎖定 → 解鎖 → 畫面還在，沒有變成白畫面
- [ ] 遊戲進行中切到別的 App 再切回來 → 同上
- [ ] 遊戲進行中接一通電話（或用計時器模擬中斷）→ 回來之後音訊還放得出來

## 三、日間／夜間

- [ ] 設定 → 外觀 → 日間：**在室外或窗邊**看一次，卡片和頁面分得開嗎
- [ ] 同上，在昏暗的房間看一次夜間版，九個選項的字讀得到嗎
- [ ] 把手機系統改成淺色 / 深色，外觀留在「跟隨系統」，開站時**不會先閃一下另一個顏色**

## 四、多人（至少兩台裝置，其中一台用行動網路）

- [ ] A 開房 → 房號出現 → B 輸入房號加入 → A 看得到 B 進來
- [ ] A 按開始 → **兩台同時聽到同一段**（這是房間最核心的假設）
- [ ] 搶答模式：兩台同時點 → 只有一台得分，兩台顯示的比分一致
- [ ] 一台中途關掉分頁 → 另一台不會卡住
- [ ] A 用 Wi-Fi、B 用行動網路，仍然連得上
- [ ] 六個人（或六個分頁）同時在房裡，還跑得動

## 五、排行榜與意見箱

- [ ] 結算頁留暱稱上傳 → 重新整理後，線上榜看得到那一筆
- [ ] 故意用同一個暱稱上傳兩次 → 榜上兩列後面**各有日期**分得開
- [ ] 左下角的信箱按得到，而且**沒有被底下那條開始鈕擋住**
- [ ] 信箱 → 給幾顆星 + 一句話 → 送出 → 顯示「收到了」
- [ ] 結算頁那一行「留個意見」、設定裡的「意見回饋」，打開的是同一個彈窗
- [ ] 到 Supabase 後台的 Table Editor，**看得到剛剛那筆意見**
- [ ] 用另一台裝置試著讀 feedback 表（例如在瀏覽器網址列打 REST 的 select）
      → 應該**讀不到**（只給寫不給讀是刻意的）

## 六、慢網路

- [ ] 用 Safari 的開發者工具（或把手機調成 3G）限速 → 開一場
- [ ] 音檔載超過三秒時，畫面會顯示「還在載入…先開始計時了」而不是卡在「載入中」
- [ ] **第二題之後幾乎不會再看到「載入中」** ——當題在播的時候已經先抓好後面兩題了。
      這一項是這份清單裡最重要的：預載靠的是 HTTP 快取（試聽檔的 Cache-Control 是 294 天），
      而無頭瀏覽器連不到 Apple，所以自動測試只驗得到「請求有發出去」，驗不到「真的變快」。
- [ ] 用開發者工具的 Network 看：第二題的音檔應該標示成 from disk cache 或 from memory cache
- [ ] 音樂終於出聲之後，那句「還在載入」會換掉

---

## 發現

做完把問題寫在這裡（一條一行，附上裝置與 iOS 版本）：

- （還沒做）

---

## 自動測試怎麼跑

```
:: 規則（81 項）
msedge --headless=new --disable-gpu --virtual-time-budget=5000 ^
       --dump-dom "file:///…/web/tests.html"

:: 版面（55 項）
msedge --headless=new --disable-gpu --allow-file-access-from-files ^
       --virtual-time-budget=20000 --dump-dom "file:///…/web/layout-tests.html"

:: 整場（14 項）
msedge --headless=new --disable-gpu --allow-file-access-from-files ^
       --autoplay-policy=no-user-gesture-required ^
       --virtual-time-budget=120000 --dump-dom "file:///…/web/playthrough.html"
```

三頁都會把 `RESULT PASS n/n` 畫進 DOM，所以 `--dump-dom` 抓得到。
題庫產生器那半邊是 `dotnet test`（44 項）。
