# 多人房間

> 這份文件回答三個問題：**三條連線路徑該用哪一條**、**每一條你得先做什麼**、
> **怎麼驗它們還活著**。

---

## 一、這是一個獨立的頁面

```
web/room.html            多人房間的畫面骨架（不從 index.html 走）
web/room.css             只有房間才有的樣式；配色與選項鈕沿用 style.css
web/js/realtime.js       即時層：三個 adapter 共用一組介面
web/js/room.js           房間邏輯：名冊、出題同步、搶答仲裁、排行榜
web/realtime-config.js   金鑰（設計上可以公開的那一把）

區域網路開房.cmd         把這台電腦變成中繼，朋友用手機連進來
tools/SongQuiz.LanServer/  那支伺服器：靜態檔 ＋ WebSocket 中繼，沒別的
```

**單人版一個檔都沒有改。** `index.html`、`style.css`、`js/app.js`、`js/rules.js`、
`js/game.js`、`js/questions.js`、`tests.html` 全部原封不動——多人做成獨立頁面就是
為了這件事：只想單人玩的人不該為多人功能付出載入成本。

單人首頁的「或和朋友開房間搶答 →」就是入口；`room.html` 也有一條回單人版的連結。

---

## 二、三條路，什麼時候用哪一條

| 路 | 誰連得到 | 你要先做什麼 | 什麼時候用 |
|---|---|---|---|
| **同機測試**（BroadcastChannel） | 同一台電腦的多個分頁 | 什麼都不用 | 自己改程式時把協定跑一遍 |
| **區域網路**（LanServer） | 同一個 Wi-Fi 底下的任何裝置 | 雙擊一個 .cmd | **朋友就在旁邊——這是預設該用的那一條** |
| **跨網路**（Supabase） | 全世界 | 註冊帳號、抄金鑰、把 `web/` 放到別人連得到的網址 | 朋友不在同一個場地 |

**先從中間那一條想。** 這個遊戲的實際場合是「幾個人在同一個房間、連同一個 Wi-Fi」，
而那個場合根本不需要外部服務：主辦人那台 Windows 電腦當中繼就夠了。

### 區域網路這一條為什麼不需要任何帳號

因為**網站和中繼是同一台機器、同一個網址**。

雙擊 `區域網路開房.cmd`，它會把 `web/` 整包當靜態網站送出去，並且在
`/ws?room=XXXX` 開一個 WebSocket 中繼。朋友打開的那個網址（`http://192.168.x.x:5000/room.html`）
本來就是從這台電腦送出去的，所以**瀏覽器早就知道中繼在哪裡**——
`realtime.js` 直接用 `location.host` 連回去，不必填 IP、不必填金鑰、不必設定檔。

「要設定什麼」的資訊如果早就在網址列裡，就不該再叫使用者抄一次。
Supabase 那條路之所以要抄金鑰，純粹是因為網站和訊息服務是兩家不同的機器。

跑起來長這樣：

```
  ────────────────────────────────────────────────
   猜歌王 · 區域網路房間　伺服器已啟動
  ────────────────────────────────────────────────

   網站資料夾：C:\Users\User\claude ai\SongQuiz\web

   叫朋友用手機瀏覽器打開這個網址（要連同一個 Wi-Fi）：

        http://192.168.50.199:5000/room.html
            （網卡：乙太網路）

   有好幾個就一個一個試——開得起來的那個就是對的。
   單人版在同一個位址的根目錄：http://192.168.50.199:5000/

   第一次跑的話，Windows 防火牆會跳出提示，要按「允許存取」，
   而且「私人網路」那個勾要打勾，不然朋友連不進來。
```

多張網卡就全部印出來讓人自己試：一台筆電同時有 Wi-Fi、有線、還可能有
WSL／Hyper-V／VPN 的虛擬網卡，從伺服器這邊看它們長得一模一樣，
程式沒辦法知道朋友的手機接在哪一個上面。猜錯的代價是「打不開，而且不知道為什麼」。

**第一次跑要在 Windows 防火牆提示上按「允許存取」，而且「私人網路」那個勾要打勾**，
不然只有自己這台連得進來。

### 那支伺服器做什麼、不做什麼

`tools/SongQuiz.LanServer` 只有兩件職責：**送靜態檔**，以及**把某個房號收到的位元組
原封不動轉給同房的其他連線**（不回送給發送者本人——那是 adapter 介面的契約）。

**它不解析遊戲訊息、不判分、不判誰先答對、不存任何東西。** 房主仍然是裁判。
這條線要守住：中繼一旦開始「懂」遊戲，它就變成第二個遊戲規則實作，
從此每次改規則都要記得改兩個地方——而且這一個是用 C# 寫的，忘了改不會有人發現，
只會在朋友面前算錯分。它就是一根水管。

房間沒人了就整間從記憶體刪掉；連線斷在一半、送到一半掛掉，都只影響那一條連線。

### `pick()` 怎麼自動決定

畫面上**沒有**「選一條線」的開關，這是刻意的：區域網路這條路的全部價值就在於
不必設定，多一個下拉選單等於把設定又還給使用者。`Realtime.pick()` 自己判：

1. 網址有 `?rt=broadcast` / `?rt=supabase` → 聽它的（既有的測試後門）。
2. 這一頁是從 `http(s)://` 載來的，**而且那個來源真的有中繼** → `LanAdapter`。
   「有沒有中繼」是打一支 `/api/ping` 問出來的，而且只認自己的招牌
   （`{"service":"songquiz-lan","relay":true}`）——不能只看「是不是 http」，
   因為放在 GitHub Pages 上的同一份檔案也是 https；也不能只看狀態碼，
   因為很多靜態空間找不到檔案時會回 200 加一頁 HTML。
   探測在 `realtime.js` 一載入就發動（逾時兩秒），所以等到有人打完暱稱按下按鈕時，
   答案早就回來了。
3. `realtime-config.js` 設了 `provider: 'supabase'` 且金鑰齊全 → `SupabaseAdapter`。
4. 其餘（`file://` 雙擊開、或什麼都沒設）→ `BroadcastChannelAdapter`。

第 2 條排在第 3 條前面，是因為「人已經從那台伺服器把這一頁打開了」本身就是
一個很明確的意思表示：他要的就是那台伺服器。

---

## 三、跨網路那一條選了 Supabase Realtime Broadcast，理由

（以下是 2026 年 9 月查證的結果，不是印象。）

### 1. 有真正的 UMD bundle，可以用傳統 `<script>` 載

```
https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.116.0/dist/umd/supabase.js
```

2.116.0 的 `package.json` 把 `jsdelivr` 與 `unpkg` 入口都指向 `dist/umd/supabase.js`，
那個檔開頭就是 `var supabase=(function(e){…`——載入後掛在 `window.supabase`，
不是 ES module。這個站不准用 module（要支援 `file://`），所以這一點是硬需求。

**版本號鎖死小版號，不要寫 `@2`。** 浮動範圍等於「上游哪天改了，房間就在客人面前
壞掉」，而這個站沒有 CI、沒有打包步驟，壞了不會有人先發現。

這個檔不寫在 `room.html` 的 `<script>` 裡，而是由 `realtime.js` 在
`provider === 'supabase'` 時才動態插入——那 218 KB 對「只想同機測試」或
「還沒設定」的人是白付的。

### 2. 匿名可用，而且是預設行為

Realtime Settings 有一個「**Allow public access to channels**」，
[官方文件寫明預設是 Enabled](https://supabase.com/docs/guides/realtime/settings)：
「no policy check runs, but anyone holding your project's anon key can subscribe to
and broadcast on any public channel」。

也就是說**玩家不需要註冊、不需要登入、不需要 session**，
拿 publishable／anon key 直接 `createClient()` → `channel()` → `subscribe()` 就能開房。
不需要建任何資料表，不需要寫任何 RLS 政策。

要收緊的時候才需要關掉那個開關，改用 `private: true` 的 channel ＋
`realtime.messages` 的 RLS 政策——那時玩家就得先有身分了。我們刻意不走那條路。

### 3. 那把 key 設計上就是公開的

[API Keys 文件](https://supabase.com/docs/guides/api/api-keys)對 publishable key 的
描述是「**Safe to expose online: web page, mobile or desktop app, GitHub actions,
CLIs, source code**」。它拿得到什麼完全由伺服器端的 RLS 與 Realtime 設定決定，
所以寫進 `realtime-config.js` 並進版控是正當的。

**真正不能外流的是 secret key**（`sb_secret_…` / `service_role`）。
那一把永遠不要出現在 `web/` 這個資料夾裡的任何檔案。

### 4. 為什麼不是 Firebase

Firebase 也有 compat 版（`https://www.gstatic.com/firebasejs/12.19.0/firebase-app-compat.js`
＋ `firebase-database-compat.js`，兩個檔都在，最新是 12.19.0），技術上可行。
但是：

- **Realtime Database 新專案預設是 locked mode**（`.read: false` / `.write: false`），
  匿名讀寫一律被擋。要嘛自己把規則改開，要嘛開啟 Anonymous Auth——
  兩條路都比 Supabase 的「預設就能用」多一步，而且第一條等於放一個全世界可寫的資料庫。
- **RTDB 是一個儲存體，不是訊息匯流排。** 房間是一場對話，結束就該什麼都不留；
  用 RTDB 做的話，每一間房都會在資料庫裡留一堆房間狀態，還要自己想辦法清。
  Supabase 的 broadcast 是純轉送，不落地。
- compat 版要載兩個檔，UMD 只要一個。

### 5. 已知的長期痛點

**Supabase 免費專案閒置 7 天會自動暫停**（要進 Dashboard 手動恢復）。
這是這個選擇唯一真正的代價。如果這個房間會擺著幾個月不用再突然要用，
記得先去 Dashboard 看一眼專案是不是還醒著。

另外：**Supabase 要在 2026 年底前淘汰 `anon` 與 `service_role` 這兩個舊 key**，
新的叫 publishable（`sb_publishable_…`）與 secret（`sb_secret_…`）。
現在兩套都還能用，但新開的專案直接拿 publishable key 就對了。

---

## 四、跨網路（Supabase）你接下來要做什麼

1. 去 <https://supabase.com> 註冊，建一個 **新專案**（Free 方案）。
   地區挑離你近的（例如 Southeast Asia / Singapore）。
2. 專案建好後進 **Project Settings → API Keys**，抄兩樣東西：
   - **Project URL**，長得像 `https://abcdefghijklm.supabase.co`
   - **publishable key**，`sb_publishable_…` 開頭
     （如果那一頁只給你 legacy 的 `anon` key，那一把也可以用，
     它是一段 `eyJ…` 開頭的 JWT）
   **不要抄 secret key／service_role key。**
3. 進 **Project Settings → Realtime**（或 Database → Realtime 設定），
   確認 **Allow public access to channels 是開著的**。新專案預設就是開的，
   看一眼是為了以後有人把它關掉時知道要來這裡看。
4. 打開 `web/realtime-config.js`，改成：

   ```js
   window.REALTIME_CONFIG = {
     provider: 'supabase',
     url: 'https://你的專案.supabase.co',
     anonKey: 'sb_publishable_你的那一把',
   };
   ```

5. 要讓別人的裝置打得開這一頁，`web/` 要放在他們連得到的地方——
   GitHub Pages、或本專案的「區域網路開房.cmd」（同一個 Wi-Fi 就夠）。

   **`file://` 雙擊開啟其實也能連上 Supabase**（實測過：SDK 載得進來、
   兩端訂閱成功、broadcast 收得到）。`file://` 唯一的問題是**別人的手機
   打不開你電腦上的檔案路徑**——所以需要的是「一個大家都連得到的網址」，
   而不是「https 才能跑」。
6. 一個人建房、把四位房號念給其他人，其他人在自己手機上輸入房號 ＋ 暱稱。

**不必做的事**：不用建資料表、不用寫 RLS 政策、不用開啟任何 Auth provider、
不用部署 Edge Function。broadcast 不碰資料庫。

流量參考（已經算過，不用重算）：5 人 10 題約 150 則訊息、30 KB，免費額度綽綽有餘。

---

## 五、即時層的介面

`room.js` 只認得這幾個成員，完全不知道背後是誰：

```js
adapter.selfId              // 這個節點的身分，房間用它認人
adapter.label               // 給人看的通道名稱（畫面上那一行狀態）
adapter.crossDevice         // 能不能跨裝置（false ＝ 只有同一台電腦）
adapter.connect(roomCode)   // → Promise<void>，連上才 resolve
adapter.send(type, payload) // 廣播一則訊息
adapter.close()
adapter.onMessage(fn)       // fn({ type, payload, from })
adapter.onStatus(fn)        // fn(status, detail)；idle/connecting/open/closed/error
```

**契約裡最重要的一條：`send` 不會把訊息回送給自己。**
這不是省事——BroadcastChannel 規格上就不派送給發送端，Supabase 的 broadcast
預設 `self: false`，兩邊本來就一樣。既然一樣就寫進契約：呼叫端自己先套用自己的動作，
這樣「本機立刻有反應」和「遠端收到訊息」走的是同一段程式碼。

三個實作：

- **`BroadcastChannelAdapter`** —— 瀏覽器內建，不需要任何帳號。
  同一台電腦多開幾個分頁就能玩完一整場。這是唯一能在沒有服務金鑰、也沒有伺服器的
  情況下端到端驗證的路徑，所以它是這個協定的**固定測試工具**，不是玩具。
- **`LanAdapter`** —— 連回這一頁自己的來源（`location.host`）的 WebSocket 中繼。
  `crossDevice` 是 true。**不寫死任何 IP**：網頁是從那台伺服器載來的，
  所以伺服器位址就是網址本身——這也正是這條路不需要設定檔的原因。
  `https` 的頁面會自動改用 `wss`（不然會被當混合內容擋掉）。
- **`SupabaseAdapter`** —— 跨網路的那一條。

`Realtime.pick()` 的優先順序寫在第二節，而且它**一定回得出東西來**：
金鑰沒填是預期中的狀態，不是錯誤，這時退回 BroadcastChannel 並把
「多人連線還沒設定」交給畫面說明白。白畫面或丟例外是最糟的處理方式——
玩的人不知道自己該去設定什麼。

斷線的表現：`LanAdapter` 連上之後才掉線（伺服器那個視窗被關掉、Wi-Fi 斷了）
會用 `onStatus('error', …)` 通報，`net-status` 那一行會照實顯示。
自己呼叫 `close()` 不算斷線——`_close()` 會先把 `onclose` 拆掉再關。

網址加 `?rt=broadcast` 可以蓋掉自動判斷，強制走同機通道。
這是留給測試的後門：就算已經接上了中繼或 Supabase，也還要能用兩個分頁把協定跑一遍。

---

## 六、房間協定

一則訊息長這樣：`{ protocol: 1, type, from, payload }`。
版本不合就整則丟掉，不要用錯的欄位名去猜對方的意思。

| type | 誰發 | 做什麼 |
|---|---|---|
| `hello` | 客人 | 我進來了，我叫什麼 |
| `roster` | 房主 | 權威名冊 ＋ 設定 ＋ 現在是等待還是已開打 |
| `start` | 房主 | 開打，設定（含**亂數種子**）一起給 |
| `ask` | 房主 | 出第 N 題，附一個不洩漏答案的題目指紋 |
| `claim` | 任何人 | 我按了，對或錯（自己在本機判的） |
| `strike` | 房主 | 某人答錯了，這一題還沒結束 |
| `award` | 房主 | 這一題誰搶到（或沒人），附**整份**比分 |
| `over` | 房主 | 結算排行榜 |
| `bye` | 任何人 | 我走了 |

`award` 傳整份比分而不是「誰加一分」：只傳增量的話，中途掉一則訊息就會讓某個人
從此永遠少一分，而且沒有人會發現。

### 所有人怎麼看到同一題

房主決定一顆整數種子，`start` 廣播出去，每個人用同一顆種子跑

```js
new Game({ mode, bank, languages, questionCount, rng: seededRng(seed), now: () => 0 })
```

題目和九個選項的順序自然一致。**沒有另外發明同步題目的機制，也沒有改 `game.js`。**

兩個細節值得記著：

1. **`Game` 在房間裡只是出題器。** 真正的計分是房間自己的（先答對的拿一分），
   `game.totalScore` 在這一頁是沒有意義的數字。
2. **每題一律餵正解、時鐘固定回 0。** 為什麼不各自傳自己的答案：
   `game.answer()` 在闖關模式會依「這一關拿幾分」決定過關或失敗，一旦每個人的分數
   不同，關卡進度就會分岔，下一題開放的語種跟著不同，畫面就再也不是同一題了。
   固定輸入之後，`Game` 的內部狀態（rng 走到哪、出過哪些歌、第幾關）在所有人身上
   逐字相同。代價只是 `totalScore` 沒有意義，而房間本來就不看它。

題庫指紋（`generatedAt` ＋ 首數）跟著設定一起傳。所有人得用同一份題庫，
否則同一顆種子也會抽出不同的歌，而畫面上看起來完全正常——只是大家在猜不同的題目。
每題的 `ask` 還帶一個題目指紋（九個選項的字數按位置加權求和，看不出答案），
不合就當場在畫面上說「這一場不算」。

### 「誰先答對」的判定

**由房主仲裁，判準是房主收到 `claim` 的先後**，不看玩家自己回報的時間。

- **沒有共同時鐘。** 各台機器的 `Date.now()` 可能差好幾秒，`performance.now()`
  的原點更是各自的頁面載入時間——兩邊的「0.8 秒」不是在量同一件事。
  做 NTP 式的校時在一個猜歌遊戲上明顯過頭。
- **玩家自報的時間等於讓他自己填分數。** 這是純靜態站，答案就在瀏覽器裡。
  「房主收到的順序」是他改不動的——改了只會讓自己的訊息更晚到。
- **房主是唯一有全序的節點。** 要有全序就得有仲裁者，不自架伺服器的話，
  房主是唯一自然的選擇。

代價，誠實講：網路慢的人吃虧，而房主的 `claim` 少走一趟網路，天生就佔便宜。
這在「不自架伺服器」的前提下沒有解，只能不要再加一層不公平——
所以房主自己的 claim 走的是同一個函式、同一個判斷，不特別優待也不特別懲罰，
而且畫面上寫明「房主裁判」，玩的人知道規則是什麼。

- **平手不存在。** 訊息一則一則進 handler，第一個把 `resolved` 翻成 true 的就是贏家。
- **答錯不能重答同一題**，但答錯不會提早結束這一題（別人還在搶）。
- **慢的人看得到「已經被搶走」。** 判定的同一刻就廣播 `award`，
  所有人立刻鎖選項、看到是誰搶到、看到正解——不用在那裡乾等十二秒。
- **時間到都沒人答對**：只有房主的計時器有效力，它宣布這一題作廢。

---

## 七、怎麼驗

### 區域網路那一條

先跑起來（`區域網路開房.cmd`，或直接
`dotnet tools\SongQuiz.LanServer\bin\Debug\net8.0\SongQuiz.LanServer.dll`），
然後開一個測試頁，裡面兩個 `<iframe src="room.html">`——
**網址一定要用 `http://localhost:5000/…` 而不是 `file://`**，
不然走的是 BroadcastChannel，等於什麼都沒驗到。

```
"/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe" ^
  --headless=new --disable-gpu --virtual-time-budget=60000 ^
  --dump-dom "http://localhost:5000/你的臨時檔.html"
```

從 http 載入的話 iframe 之間本來就同源，**不需要** `--allow-file-access-from-files`。

測試頁怎麼知道該按哪一顆：讀 iframe 裡 `#player` 的 `src`（那就是正解那首歌的
試聽網址），在同一個 iframe 的 `window.SONG_BANK` 裡找回那首歌，
再用它自己的 `window.labelOf()` 算出選項上的字。`room.js` 不必為了測試開任何後門。

**這一條多了兩個 BroadcastChannel 沒有的坑，都是虛擬時鐘造成的，兩個都踩過：**

1. **虛擬時鐘會直接跳過 WebSocket 的握手。** `--virtual-time-budget` 的時鐘只在
   「沒有網路請求在飛」的時候前進，而 WebSocket 的握手不算那種請求——
   結果是第二條連線還沒握完手，虛擬時間就燒完了，Chrome 就把 DOM dump 出來。
   實測：不釘住的話只有第一個 `WebSocket` 會 open，第二個永遠停在 CONNECTING，
   畫面上看起來就是「客人卡在連線中」，而且那是測試環境的假象，不是程式的錯。
   解法是整場保持**一個** `fetch('api/ping')` 滾動在飛，把虛擬時鐘釘住，
   讓這一場用真實時間跑。
2. **那個 fetch 只能留一個。** 開四個併發會把瀏覽器對同一個 host 的連線池佔滿，
   WebSocket 的握手反而排不進去——實測 4 個併發時兩條連線都開不起來。

因為虛擬時鐘被釘住了，每題揭曉後的 `REVEAL_MS`（3.5 秒）會真的等 3.5 秒。
測試頁在房主那一格盯著 `#btn-next-q` 的 `disabled`，一解鎖就按——
那是房間本來就有的「下一題」，走的是同一段程式碼，不是為測試開的後門。

驗過的事情（兩個分頁、五題、競速，`RESULT PASS`）：
兩邊都自動選中 `LanAdapter`（不是退回 BroadcastChannel）、建房→加入→開始、
五題的正解與九個選項順序兩邊逐字相同、只有客人按的三題分數全落在客人身上、
兩個人同時搶的兩題各只有一個人得分（總分 5 ＝ 題數）、兩邊排行榜一致。
最後 `/api/ping` 的 `rooms` 回到 0，表示空房有被清掉。

### 同機那一條

`BroadcastChannelAdapter` 在同一個 `file://` 來源之間是通的，所以可以在一個頁面裡
開三個 `<iframe src="room.html?rt=broadcast">` 當三個玩家，用無頭 Edge 跑完一整場。

```
"/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe" ^
  --headless=new --disable-gpu --allow-file-access-from-files ^
  --autoplay-policy=no-user-gesture-required --virtual-time-budget=300000 ^
  --dump-dom "file:///C:/.../web/你的臨時檔.html"
```

`--allow-file-access-from-files` 一定要加，否則 `file://` 下的 iframe 之間讀不到 DOM。

寫這種測試頁的三個坑，都踩過了：

1. **按下選項必須和「題目畫出來」發生在同一個 task 裡**，不要用 `setInterval` 輪詢。
   `--virtual-time-budget` 的虛擬時鐘會整段跳躍，輪詢的 40 毫秒可能被直接跳過十二秒
   ——那時題目已經逾時，測出來的「沒人搶到」是測試環境的假象，不是程式的錯。
   正確做法：在每個 iframe 的 `#choices` 掛 `MutationObserver`，DOM 一畫好就按下去。
2. **逾時要看 `Date.now()` 真正過了多少，不要數自己跳了幾次。**
   虛擬時鐘忽快忽慢，數 tick 兩個方向都會誤判。
3. **測試頁可以自己開同一條 `BroadcastChannel` 當「旁聽者」**，
   用 `start` 帶的種子重算出每題正解——這樣才知道該按哪一顆，
   而 `room.js` 裡不必為了測試開任何後門。

另外要知道：無頭環境下 BroadcastChannel 偶爾會整個 process 一則訊息都不派送
（約每十次一次），測試頁會停在「加入房間」那一步。重跑就好，那不是程式的問題。

驗過的事情（三個玩家、五題、競速，每次 63 項全綠）：
建房→加入→開始、三個人的題目與選項順序逐字相同、先答對的拿分而慢的完全不能作答、
答錯出局但不提早結束該題、同時搶只有一個人得分、沒人答對不算分、排行榜正確。

闖關模式的「同一顆種子＝同一串題」另外單獨驗過：
五語種每關 5 題 → 30 題、兩語種每關 10 題 → 30 題、單語種 5 題 → 5 題，
兩個 `Game` 逐題逐選項一致，換一顆種子就換一整套題目。

---

## 八、已知的限制（沒做，不是忘了）

- **房主離開房間就散了。** 沒有重新選房主，也沒有斷線重連。
  房主重新載入頁面就是一間新房。
- **已經開打就不收人。** 中途加入的人沒有跑過前面幾題，名次會看起來像作弊，
  不如老實請他等下一場。
- **分數還是不可信。** 判分在瀏覽器裡（單人版本來就是這個取捨），
  房主只仲裁「誰先」，不驗「對不對」——因為驗了也一樣不可信。
  要可信就得把判分搬回伺服器，那是另一件事。
- **闖關模式在房間裡不會「過不了關」**（見上面「每題一律餵正解」）。
  它在這裡的意義只剩「一關解鎖一個新語種」的出題方式與更長的題數。
- **中繼沒有身分驗證。** 同一個 Wi-Fi 底下，任何人猜中四位房號就能連進來聽
  （也能發訊息）。這是刻意的：要驗身分就得先有帳號，而這條路存在的全部理由
  就是不要帳號。真的介意的話，換 Wi-Fi 比加密碼有效——房間只活幾十分鐘，
  而且裡面只有歌名。
- **伺服器那個視窗關掉，房間就斷了。** 沒有重連。`LanAdapter` 會如實報
  「和中繼斷線了」，但不會自己接回來——這和「房主離開就散了」是同一個決定：
  一場遊戲幾十分鐘，重開一間比寫一套重連狀態機便宜。
