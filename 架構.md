# SongQuiz 架構

> 這份文件只回答一個問題：**要改東西的時候，怎麼樣才不會牽一髮動全身。**

---

## 一、依賴方向（由編譯器強制，不是靠自律）

```
                 SongQuiz.Common          零依賴：時鐘、洗牌
                        ↑
                 SongQuiz.Quiz            題庫：歌、誘餌、歌名清洗、出題
                        ↑
                 SongQuiz.Game            規則：計分、關卡、回合狀態機
                        ↑
                 SongQuiz.Server          minimal API + wwwroot 前端
```

`tools/SongQuiz.BankBuilder` 掛在 Quiz 旁邊（只依賴 Common 與 Quiz），
它是離線工具，不是伺服器的一部分。

**鐵律：Quiz 不知道有「遊戲」這回事。** 它只會出題，不知道十二秒、不知道六關、
不知道一題值幾分。所以改計分公式不會動到出題，改出題規則不會動到計分。

---

## 二、答案在哪裡：這是整個設計的核心

```
瀏覽器                          伺服器
  │                               │
  │  POST /api/games/{id}/question│
  │ ─────────────────────────────▶│  出題，記下「這一題是幾點幾分出的」
  │                               │
  │  ◀── 九個選項 + 試聽網址 ──────│  沒有正解、沒有分數公式、沒有秒數起點
  │                               │
  │  POST …/answer {choiceId: 3}  │
  │ ─────────────────────────────▶│  比對正解、用伺服器的時鐘算分
  │  ◀── 對錯 + 正解 + 得分 ───────│
```

**為什麼不放前端**：正解一旦進到瀏覽器，就等於交給玩家。這是擺攤要排行榜的遊戲，
分數必須是可信的。所以 `QuestionView` 這個型別裡**刻意沒有** `AnswerId`
——不是「記得別送出去」，是**根本送不出去**。

正解只開給測試專案（`InternalsVisibleTo`，見 `src/SongQuiz.Game/AssemblyInfo.cs`），
因為測試必須能驗「答對真的算分、答錯真的不算」。

計時同理：秒數由伺服器從出題那一刻算起，扣掉 250 毫秒的網路寬限
（`Rules.LatencyGrace`），前端那條計時條只是給人看的。

---

## 三、音檔：我們不碰

題庫裡存的是 Apple Music 官方 30 秒試聽的網址，播放時由 Apple 的伺服器
直接串到瀏覽器。**本專案不下載、不轉存、不代理、不快取任何音訊。**

好處不只是省頻寬：音檔的授權問題留在原本就有授權的人身上。

---

## 四、題庫怎麼來

```
tools/SongQuiz.BankBuilder
        │  讀 Artists.cs 的演出者名單（語種是名單本身決定的）
        │  循序呼叫 iTunes Search API，每次間隔 400 毫秒
        ▼
src/SongQuiz.Server/data/bank.json      512 首可出題 + 765 個誘餌
        │
        ▼
伺服器啟動時讀一次進記憶體
```

**為什麼用演出者當入口而不是排行榜**：排行榜混語種，語種分類只能靠猜；
從演出者出發，語種在名單裡就已經確定。而闖關模式整個玩法都建立在語種分類上。

**為什麼要「誘餌」**：誘餌是只當錯誤選項、永遠不會被出題的歌名。
沒有它們，玩家會發現「選項裡出現過的歌才可能是答案」，然後改用排除法刷分。
誘餌讓九個選項一樣可信。

題庫更新的流程是「跑 BankBuilder → 重啟伺服器」。一天之內題庫不會變，
所以不為熱重載付出複雜度。

---

## 五、對局狀態放記憶體

`SessionStore` 是一個 `ConcurrentDictionary`，兩小時沒人碰就掃掉。

一場遊戲三分鐘，伺服器重開就重玩。為它架資料庫換不到任何東西。
**要留排行榜的時候再說**——那是新的需求，到時候該加的是一張表，
而不是現在先把對局寫進資料庫。

---

## 六、關卡與語種解鎖

六關的門檻與語種都寫在 `Rules.Stages` 一個陣列裡。要調難度只改那一個地方。

| 關 | 門檻 | 語種 | 新解鎖 |
|---|---|---|---|
| 1 | 4000 | 華語 | — |
| 2 | 5000 | 華語、台語 | 台語 |
| 3 | 6000 | ＋西洋 | 西洋 |
| 4 | 7000 | ＋韓語 | 韓語 |
| 5 | 8000 | ＋日語 | 日語 |
| 6 | 9000 | 全部 | — |

**新解鎖的語種保證至少出一題**（`GameSession._guaranteed`）。
否則第三關解鎖了西洋歌，玩家卻可能整關都在聽華語，那這一關等於沒有意義。

過關的瞬間就推進到下一關，不等到下次出題——否則「過關」到「出下一題」之間，
`Stage` 講的是舊關卡，任何讀它的人都會慢一拍。回報給前端的
`AnswerOutcome.Stage` 則是**剛打完的那一關**，因為畫面要寫「第 1 關過關！」。
