# SongQuiz 架構

> 這份文件只回答一個問題：**要改東西的時候，怎麼樣才不會牽一髮動全身。**

---

## 一、這是一個純靜態站

```
web/
  index.html          畫面骨架
  style.css
  js/rules.js         規則與計分（純資料 + 純函式，不碰 DOM）
  js/questions.js     出題：九選一、同語種誘餌、不重複
  js/game.js          回合狀態機：闖關／競速、關卡推進
  js/app.js           單人版：碰 DOM 與 <audio>
  js/prefetch.js      音檔預載（單人與多人共用一份，各生一個實例）
  data/bank.js        題庫（產生物，但有進版控——見第五節）
  tests.html          規則測試，打開就跑
tools/
  SongQuiz.BankBuilder/   C# 離線工具：產生 data/bank.js
```

**沒有伺服器，也沒有打包工具。** 雙擊 `web/index.html` 就能玩，整個 `web/`
資料夾丟到任何靜態空間（GitHub Pages 之類）也能玩。

### 為什麼是傳統 script 而不是 ES module

因為要支援「雙擊 index.html」。`file://` 底下 `type="module"` 會被瀏覽器的
CORS 規則擋掉，`import` 直接失敗。所以每個 js 檔是傳統 script，各自掛一個
全域名稱（`window.Rules`、`window.QuestionMaker`、`window.Game`），
由 `index.html` 決定載入順序。

### 為什麼題庫是 `.js` 而不是 `.json`

同一個理由：`file://` 底下 `fetch('data/bank.json')` 會被 CORS 擋，
而 `<script src="data/bank.js">` 不會。所以題庫檔長這樣：

```js
window.SONG_BANK={generatedAt:"…", tracks:[…], decoys:[…]};
```

參考站（台大流唱社的 ntupm-songguesser）也是這麼做的，不是巧合——
這是靜態猜歌站繞開 file:// 限制的唯一乾淨解。

檔案裡實際存的是**陣列**，由檔案自己在最後一行展開成上面那個形狀：

```js
(function(){var b={languages:[…], urlPrefix:"…", tracks:[[id,"歌名","歌手",0,"網址中段"],…]};
 window.SONG_BANK={tracks:b.tracks.map(…), …};})();
```

題庫是每個玩家一進站就要下載的東西，而現場多半是手機網路。物件形式的 JSON 會把
`id`/`title`/`artist`/`language`/`previewUrl` 這五個鍵在每一首歌重複一次（約 48 bytes），
試聽網址的共同前後綴又是每首 75 bytes，中文預設還會被逃逸成 `\uXXXX`（一個字 6 bytes，
UTF-8 只要 3）。兩千首下來這三項就是好幾百 KB。

**展開後的形狀刻意和以前一模一樣**，所以 `app.js`、`room.js`、測試都不用改——
存法是實作細節，不該漏到遊戲邏輯裡。


---

## 二、分層：誰可以知道誰

```
rules.js       ← 只有數字與計分公式。不知道有「一場遊戲」這回事
   ↑
questions.js   ← 只會出題。不知道十二秒、不知道有幾關、不知道一題值幾分
   ↑
game.js        ← 回合、計分、關卡推進。不知道畫面長什麼樣
   ↑
app.js／room.js  ← 只有這兩個（＋它們共用的 prefetch.js）碰 DOM 與音訊
```

**鐵律：`rules.js`、`questions.js`、`game.js` 裡不准出現 `document`。**
這不只是整潔——`tests.html` 能在無頭瀏覽器裡驗 61 項規則，
就是因為這三層完全不依賴畫面。

時間與亂數都從外面注入（`new Game({ mode, bank, languages, questionCount, now, rng })`），
所以測試可以用假時鐘精準測「11.9 秒答對得 504 分、12.0 秒得 0 分」，
也可以固定種子重現同一題。

---

## 三、答案在瀏覽器裡：這是刻意的取捨

純靜態就代表題庫、正解、計分公式全都在玩家的瀏覽器裡。
打開開發者工具就能看到答案——**和參考站一樣，這是靜態站的代價。**

換到的是：不必維護伺服器、不必付主機費、可以丟 GitHub Pages、
雙擊就能玩、離線也能玩（只要音檔還載得到）。

**什麼時候必須推翻這個決定**：要做「可信的排行榜」的時候。
一旦分數要跨人比較，判分就必須回到伺服器，否則排行榜第一名
只是最會改 JavaScript 的人。這個專案的第一版曾經是
「ASP.NET Core 伺服器判分」，那份程式還在 git 歷史裡（提交「骨幹：兩種模式的猜歌遊戲，答案留在伺服器」），
真的要做排行榜時可以翻回去參考。

---

## 四、音檔：我們不碰

題庫裡存的是 Apple Music 官方 30 秒試聽的網址，播放時由 Apple 的伺服器
直接串到瀏覽器。**本專案不下載、不轉存、不代理、不快取任何音訊。**

好處不只是省頻寬：音檔的授權問題留在原本就有授權的人身上。

---

## 五、題庫怎麼來

```
tools/SongQuiz.BankBuilder（C#，離線跑）
        │
        │  ① 讀六份 Apple 排行榜（Charts.cs）拿演出者，依名次排序
        │     ＋ 接上手打的備源名單（Artists.cs）
        │  ② 對每位演出者呼叫 iTunes Search API，每次間隔 400 毫秒
        │     有試聽網址的收進題庫，其餘降級成誘餌
        │  ③ 收到語種上限就停
        ▼
web/data/bank.js        每語種 400 首可出題 ＋ 500 個誘餌（合計 2,000 ＋ 2,500）
```

**為什麼是「榜單拿人、Search 拿歌」兩段式**：

一開始只有手打的演出者名單。它的問題不是不能用，是**不會自己更新**，
而且「誰紅」是人猜的。榜單是真的播放排行，每個月重跑就會換人。

但榜單不能直接當題庫：一份上限 100 首，太少，而且題目會集中在那幾首芭樂歌。
它指到的 50～70 位演出者才是有價值的部分——再用 Search API 各撈十幾首，就有幾百首。

**語種分類怎麼解決的**：這本來是排行榜最大的問題（台灣的榜上華語台語混在一起）。
後來發現 Apple 的 feed 自帶曲風 id，而且分得開：1253 華語流行樂、1254 台灣流行樂、
1252 韓國流行、1251 粵語流行。所以改用「一個曲風榜 = 一個語種」，
分類不是猜的，是 Apple 標的。

**手打名單還留著**，現在是備源：補榜單撈不滿的語種（韓國商店的 RSS 實測是空的），
以及補經典曲——榜單只看「現在在紅」，但長青曲才是最有把握被認出來的歌。


**為什麼要「誘餌」**：誘餌是只當錯誤選項、永遠不會被出題的歌名。
沒有它們，玩家會發現「選項裡出現過的歌才可能是答案」，然後改用排除法刷分。
誘餌讓九個選項一樣可信。

**為什麼題庫產生器留在 C#**：它不是遊戲的一部分，玩家永遠不會執行它。
而這台機器有 .NET SDK、沒有 Node，用 C# 寫反而是阻力最小的路。
歌名清洗的規則（砍掉 `(feat. …)`、`（電視劇片尾曲）` 這類註記）也在那裡，
連同它的 14 個測試。

---

## 六、三個模式，與「設定決定規則」

開場挑三件事：**語種（多選）、題數、模式**。這三個值一路傳到 `new Game({...})`，
規則層沒有任何寫死的預設值在背後偷偷生效。

| 模式 | 計分 | 題數的意思 |
|---|---|---|
| 闖關 | 答對 500＋最多 500 速度獎勵 | **每關**題數 |
| 競速 | 同上 | 整場題數 |
| 積分 | 500 × 連對倍率（1→5 封頂），**不看速度** | 整場題數 |

積分模式刻意不看速度：它獎勵的是**連續答對**，秒答和第十一秒答對同分，
十二秒只是「還算不算答到」的界線。答錯或逾時連對歸零。

### 闖關的關卡是算出來的，不是寫死的

`Rules.stagesFor(languages, questionCount, mode)` 現場生成關卡表：

- k 個語種 → **k + 1 關**，最後一關是全語種總決賽；k = 1 時只有一關
- 第 i 關累積開放前 i 個語種，第 i 關（i ≥ 2）新解鎖第 i 個
- 語種一律照 `Rules.LANGUAGES` 的**難度順序**重排（華語最熟、日語最生），
  不照玩家勾選的先後——先勾日語不代表日語該當第一關
- 門檻＝該關滿分的 40%～90% 線性插值；只有一關時取 60%

所以「選五語種、每關 10 題」算出來就是六關、門檻 4000→9000，
剛好是這個遊戲原本寫死的那張表——**舊行為變成新公式的一個特例**，
測試裡就是這樣驗沒有回歸的。

### 開打前就知道湊不湊得出題

`Rules.capacityFor(bank, languages, questionCount, mode)`。闖關要算**整場**、
而且**逐關**檢查：第二關湊不出來，第六關的歌再多也救不了（關卡的語種層層包含，
所以逐關比對「前 i 關的總需求 ≤ 第 i 關可用的歌數」就是完整判準）。
不夠就不讓按開始，並講出差幾首。

玩到第三關才發現沒歌可出，是最糟的收場方式。

**新解鎖的語種保證至少出一題**（`game.js` 的 `guaranteed`）。
否則第三關解鎖了西洋歌，玩家卻可能整關都在聽華語，那這一關等於沒有意義。
測試用「華語 400 首、台語 1 首」的偏斜題庫驗這條——隨機幾乎抽不到，
只有保證機制會讓它出現。

過關的瞬間就推進到下一關，不等到下次出題——否則「過關」到「出下一題」之間，
`game.stage` 講的是舊關卡，任何讀它的人都會慢一拍。但回報給畫面的
`outcome.stage` 是**剛打完的那一關**，因為要寫「第 1 關過關！」。

---

## 七、測試怎麼跑

這一邊刻意不引進 JS 測試框架（引進就等於把建置流程綁進一個純靜態站）。
`web/tests.html` 是一個自己寫的極小跑法：打開就跑，結果渲染進 DOM，
最上面一行固定是 `RESULT PASS 68/68`。

無頭驗證（CI 也可以用同一條）：

```
msedge --headless=new --disable-gpu --virtual-time-budget=5000 ^
       --dump-dom "file:///…/web/tests.html"
```

版面另有一份 `web/layout-tests.html`：它把 `index.html` 用 390px 與 1200px 兩種寬度
載進 iframe，量實際排出來的尺寸。跟玩法測試分開的理由有兩個——它慢（要真的排版），
而且從 `file://` 跑必須加 `--allow-file-access-from-files`，否則讀不到 iframe 裡的文件。

```
msedge --headless=new --disable-gpu --allow-file-access-from-files ^
       --virtual-time-budget=20000 --dump-dom "file:///…/web/layout-tests.html"
```

C# 那半邊（歌名清洗）照常 `dotnet test`。
